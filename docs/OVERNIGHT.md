# Overnight 2026-08-15 — login + LLM + chat pane

Human away. Defaults: local only, no GitHub, no real mailbox password, Windows keyring when we store secrets.

**Agent:** Hermes-*shaped* (chat + tools + confirm). Not a Hermes process fork. Lean: cap history, send current thread + user text only, one local model call per turn.

**UI:** Thunderbird 3-pane. Pane 3 = read + compact agent chat (not a button strip pretending to be Hermes).

**Settings:** Add mail account. Add LLM (Ollama default, BYOK optional).
