Folders drawer (≤720): `.folders-toggle` + `.folders.open` slide from the left with a moss tick when open; olive dimmer is box-shadow, no extra DOM.
Keys: `.keys` is a settings-like instrument card (small-caps label, mono cheat sheet, olive wash), not a toast.
Move-to: `.move-to` matches toolbar buttons — panel-2, 2px radius, muted caret, disabled opacity.
Threat: `.threat` is a compact mono banner (danger rust / caution copper / ok lichen) with no left accent rail.
Toolbar wrap: `.toolbar` wraps inside an `auto` top row so 12 actions + select stay on-canvas; phone takes a full wrap row.
